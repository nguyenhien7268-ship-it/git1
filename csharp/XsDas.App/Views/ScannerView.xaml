<UserControl x:Class="XsDas.App.Views.ScannerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                          Text="🔍 Bridge Scanner"/>
                <TextBlock Text="{Binding StatusMessage}" 
                          Foreground="{DynamicResource MaterialDesignBody}"
                          Margin="0,8,0,0"/>
            </StackPanel>
        </materialDesign:Card>
        
        <!-- Action Bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="🔍 Scan Bridges" 
                    Command="{Binding ScanCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0"/>
            <Button Content="✅ Import Selected" 
                    Command="{Binding ImportSelectedCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0"/>
            <TextBlock Text="{Binding FoundTotal, StringFormat='Found: {0}'}" 
                      VerticalAlignment="Center"
                      Margin="16,0,8,0"/>
            <TextBlock Text="{Binding ExcludedCount, StringFormat='Excluded: {0}'}" 
                      VerticalAlignment="Center"
                      Foreground="Gray"/>
            <ProgressBar IsIndeterminate="True" 
                        Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Width="100" Height="4" Margin="16,0,0,0"/>
        </StackPanel>
        
        <!-- Candidates List -->
        <materialDesign:Card Grid.Row="2">
            <DataGrid ItemsSource="{Binding Candidates}"
                     AutoGenerateColumns="False"
                     CanUserAddRows="False"
                     SelectionMode="Extended">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="60"/>
                    <DataGridTextColumn Header="Streak" Binding="{Binding Streak}" Width="60"/>
                    <DataGridTextColumn Header="Rate %" Binding="{Binding Rate, StringFormat=N1}" Width="70"/>
                    <DataGridTextColumn Header="STL" Binding="{Binding Stl}" Width="100"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
        
        <!-- Status Bar -->
        <materialDesign:Card Grid.Row="3" Padding="8" Margin="0,8,0,0">
            <TextBlock Text="{Binding Candidates.Count, StringFormat='Total Candidates: {0}'}"
                      FontSize="12"/>
        </materialDesign:Card>
    </Grid>
</UserControl>
