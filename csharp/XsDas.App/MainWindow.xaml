<Window x:Class="XsDas.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:XsDas.App.Views"
        mc:Ignorable="d"
        Title="XS-DAS - Lottery Analytics System" 
        Height="900" Width="1400"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <materialDesign:DialogHost Identifier="RootDialog">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Menu Bar -->
            <materialDesign:ColorZone Grid.Row="0" 
                                      Mode="PrimaryMid" 
                                      Padding="16"
                                      materialDesign:ElevationAssist.Elevation="Dp4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ChartBar" 
                                               Width="32" 
                                               Height="32" 
                                               VerticalAlignment="Center"
                                               Margin="0,0,16,0"/>
                        <TextBlock Text="XS-DAS" 
                                 VerticalAlignment="Center"
                                 Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                    </StackPanel>

                    <TextBlock Grid.Column="1" 
                             Text="Lottery Analytics System - C# Migration" 
                             VerticalAlignment="Center"
                             HorizontalAlignment="Center"
                             Style="{StaticResource MaterialDesignBody1TextBlock}"
                             Opacity="0.7"/>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="Settings">
                            <materialDesign:PackIcon Kind="Cog"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="About">
                            <materialDesign:PackIcon Kind="Information"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:ColorZone>

            <!-- Main Content Area -->
            <views:DashboardView Grid.Row="1"/>
        </Grid>
    </materialDesign:DialogHost>
</Window>
