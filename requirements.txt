# Testing & Linting
pytest==7.4.3
pytest-cov==4.1.0
flake8==6.1.0

# Data Science & Analysis
pandas==2.1.3        # Dùng cho xử lý dữ liệu khung (DataFrames)
numpy==1.26.2        # Dùng cho tính toán số học
matplotlib==3.8.2    # Dùng cho biểu đồ trong Dashboard
seaborn==0.13.0      # Dùng cho trực quan hóa nâng cao

# Machine Learning
scikit-learn==1.3.2  # Dùng cho các thuật toán ML cơ bản & Scaler
xgboost==2.0.2       # Dùng cho mô hình dự đoán cốt lõi
joblib==1.3.2        # Dùng để lưu/tải mô hình (.joblib)

# Note: tkinter is part of Python standard library on most systems
# If UI doesn't work, install python3-tk via system package manager